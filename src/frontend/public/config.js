// Default runtime config for local dev / build. The container entrypoint
// overwrites this file from config.template.js using the BACKEND_URL env var.
window.BUILDINGASSIST_CONFIG = { backendUrl: "" };
