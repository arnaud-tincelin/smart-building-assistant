I need the assistant view to be a chat view with the agent. It shall display the conversation history (with a scrollbar if the conversation is too long) and for each answer, it shall display which model was used to reply by the agent.
it must support markdown
include playwright test to test the UI (playwright skill)
