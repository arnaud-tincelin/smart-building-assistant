I need the assistant view to be a chat view with the agent. It shall display the conversation history and for each answer, it shall display which model was used to reply by the agent (if the data is easily available)
it must support markdown
prepare a github issue, follow the best practices in term of specification redaction (acceptance criteria, ...)
include playwright test to test the UI (playwright skill)
